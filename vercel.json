{
  "buildCommand": "npm run --workspace=@maelstorm/shared build && npm run --workspace=@maelstorm/app build:web",
  "installCommand": "npm install && cd packages/app && npm install",
  "framework": null,
  "outputDirectory": "packages/app/dist"
}
